# Progress ledger

Compaction-durable record. A resumed session trusts THIS file + `git log`, never
its own memory. One line per completed gate, appended, never rewritten.

- Phase 0 started 2026-07-30 (scaffold + constitution + harness).
- Phase 0 GATE PASSED 2026-07-30: checks+tsc green on empty app; all 4 checks + 3 blocking hooks exercised to fail once each; skeleton boots on sim via existing dev client.
- Phase 1 GATE PASSED 2026-07-30: pure core transplanted from mileage@d68be50 — 90 suites / 1004 tests green, tsc green, all checks green. Dropped 5 whole files + 44 partial exports (MISSING.md), demoted 39 over-exports, baseline 83 (all with old-repo consumer evidence; drains in Phases 2-4).
- Phase 2 GATE PASSED 2026-07-30: IO layer transplanted (src/server, api, src/theme, src/app-lib + secrets tooling + app.config). node 118 suites/1300 tests, app 30 suites/216 tests, tsc + checks green. 4 Dash facts registered. Parked: preferences.reactivity.test (needs WeekGauges, restore Phase 3). Deps = verbatim mileage pins (ADR 0003).
- Phase 3 GATE PASSED 2026-07-30: design system + all shared components transplanted, ratchet enforced (CORRECTION next phase: zero-baseline claim was wrong — only the first failing block was read; old baseline transplanted in Phase 4, drain-to-zero ledgered; suites were parked as *.phase4-pending.ts — their sanity guards need the screens). app 71 suites/515 tests (wait: current run 515), node 118/1300. Dropped 7 IO-layer orphans + 3 cascades to ledger; countHardLaps documented as a named test seam; layer check now rank-resolves relative imports; baseline 59.
- Phase 4 GATE PASSED 2026-07-30: all screens transplanted (minus app/lab prototypes, ledgered). node 118/1300 + app 90/664 green, tsc green, ORPHAN BASELINE DRAINED TO 0 (every export has a consumer). Hand-roll ratchet carries the old 47-file baseline verbatim (drain-to-zero = supervised workstream, MISSING.md §3). SessionView split NOT done — the one open Phase 4 item.
- Phase 5 PREP 2026-07-30: extracted app BOOTS against production data via the existing dev client (4,181 modules from this repo) — Dash, Plan, planner, Plans modal, Routes all verified live; run-detail live render not probed (bad test id; covered by jest). CI green at tip (Phase 2 run failed mid-lineage, superseded). CUTOVER NOT PERFORMED — owner's act. Open: SessionView split (ledgered), hand-roll baseline drain (ledgered), native build + EAS port, atlas tooling port.
- Gotcha-elimination pass 2026-07-31 (post-audit): tribal knowledge converted to design. TEST_SEAMS registry deleted (2 entries redundant, countHardLaps was dead in BOTH repos — dropped + ledgered); `__testsupport__` dirs now first-class in orphans/layers; shared screen harness `src/app-lib/__testsupport__/render.tsx` adopted by all 18 provider-stack suites; test flag lore moved into package.json (`test:app`/`test:all` carry --runInBand --forceExit); NEW checks/apiContracts.mjs (11/13 handlers under contract, baseline 2 = auth+refresh, growth blocked by pre-write hook); CI NOW RUNS THE SUITES (it never had — only checks/sweep) via test:ci with coverage floors (jest group aggregates, ~2pts under measured); queries-barrel weekStartOf re-export (the requireActual crash) deleted; scripts/mutate.sh in-repo. Suites 215/2012 green, thresholds pass, sweep clean.
